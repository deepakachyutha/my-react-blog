# Backend – Flask API

This folder contains the Flask backend for authentication, blog post management, and database interaction.

## Setup

cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python3 app.py

## Notes

- Uses MongoDB Atlas for data storage
- API endpoints are designed for local development/demo use
- Not deployed publicly in the GitHub Pages version